import { ProductToCartRequest } from './product-to-cart-request';

describe('ProductToCartRequest', () => {
  it('should create an instance', () => {
    expect(new ProductToCartRequest()).toBeTruthy();
  });
});
